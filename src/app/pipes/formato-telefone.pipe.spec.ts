import { FormatoTelefonePipe } from './formato-telefone.pipe';

describe('FormatoTelefonePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatoTelefonePipe();
    expect(pipe).toBeTruthy();
  });
});
