import { LetraFormatPipe } from './letra-format.pipe';

describe('LetraFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new LetraFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
